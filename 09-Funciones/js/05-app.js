function sumar(a,b) { // a y b son parametros de la funcion
  console.log(a + b);
}

// Funcion con argumentos. 
sumar(true,true);

function saludar(nombre,apellido){
console.log(`Hola ${nombre} ${apellido}`)
}

saludar('Esteban','Zamora');
saludar();
